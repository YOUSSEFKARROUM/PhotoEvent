# Configuration de la base de données MongoDB
MONGODB_URI=mongodb://localhost:27017/photoevents

# Clé secrète JWT pour l'authentification (OBLIGATOIRE en production)
JWT_SECRET=votre_cle_secrete_jwt_tres_longue_et_complexe_ici

# Configuration Redis (optionnel - pour les queues)
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_ENABLED=true

# Configuration du serveur
PORT=5000
NODE_ENV=development

# Configuration des uploads
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# Configuration DeepFace (optionnel)
DEEPFACE_ENABLED=true

# Désactiver Redis si nécessaire
DISABLE_REDIS=false
QUEUE_MODE=redis

